<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.deemons.supermarketassistant">

    <application
            android:name=".base.App"
            android:allowBackup="true"
            android:icon="@mipmap/icon_app"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/DefAppTheme">
        <activity android:name=".mvp.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".mvp.activity.ScanCodeActivity" android:screenOrientation="portrait"/>

        <activity android:name=".mvp.activity.ScanCodeHistoryActivity"/>

    </application>

</manifest>